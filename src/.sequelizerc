const path = require('path');

module.exports = {
  "config": path.resolve('./config', 'config.json'),
  "models-path": path.resolve('./infrastructure/models'),
  "seeders-path": path.resolve('./infrastructure/seeders'),
  "migrations-path": path.resolve('./infrastructure/migrations')
};